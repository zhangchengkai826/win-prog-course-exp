<Window x:Class="zckclient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:zckclient"
        mc:Ignorable="d"
        Title="zck client" Height="200" Width="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition Height="*" />
            <RowDefinition Height="20" />
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Background="Bisque">
            <TextBlock Text="客户端进程" HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Grid>
        <Grid Grid.Row="1">
            <TextBox Name="Msg" Background="Azure" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Text="" />
        </Grid>
        <Grid Grid.Row="2">
            <Button Click="Send" Background="Cornsilk">
                <TextBlock Text="发送" />
            </Button>
        </Grid>
    </Grid>
</Window>
